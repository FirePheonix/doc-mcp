/**
 * doc-mcp - Core Module Exports
 */

export { resolveConfig, validateConfig, createDefaultConfig, mergeConfigs, isPathVisible } from './config';
export { createLogger, silentLogger } from './logger';
